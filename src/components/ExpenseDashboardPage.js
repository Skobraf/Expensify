import React from 'react';

const ExpenseDashboardPage = () => {
	return (
		<div>
		this is dashboard <NavLink  to="/create">Go to create</NavLink>
		</div>
		);
}

export default ExpenseDashboardPage;